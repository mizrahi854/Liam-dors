# סקשן `#system` — סרט הדלת מהסרטון של הלקוח

עודכן: 17.09.2026. תקשורת עם המשתמש: **בעברית בלבד**.

## מה נבנה

הסקשן `#system` באתר (`site/dist/index.html`) הוחלף. במקום הקנבס עם לוח הסטוריבורד הישן,
עכשיו יש **סרט בגלילה** שמורכב מפריימים אמיתיים של הסרטון שהלקוח שלח.

| רכיב | נתיב |
|---|---|
| נגן הגלילה | `site/dist/scene/story3d.js` |
| עיצוב | `site/dist/scene/story3d.css` |
| פריימים (117, ‏586×716, ‏WebP) | `site/dist/media/door-scene/0000–0116.webp` + `manifest.json` |
| מקור | `/Users/yanaimizrahi/Downloads/gemini_generated_video_A335E975.mov` (‏720×896, ‏221 פריימים, ‏24fps) |
| הפריימים שהלקוח שלח | `assets/fraim/` |

## למה רק 117 פריימים מתוך 221

מפריים ~120 ואילך הסרטון **צורב לתוך התמונה כתוביות משלו** (שלדת אלומיניום מחוזקת,
בידוד תרמי ואקוסטי, חיפוי תיצפי / STONE FINISHT וכו'). שתי בעיות:

1. יש שם שגיאת כתיב — „חיפוי תיצפי" ו־„STONE FINISHT" כפול.
2. אי אפשר לצייר קווים וכיתוב נקיים מעל כיתוב צרוב.

לכן בשימוש רק הקטע הנקי, פריימים 0–116: דלת סגורה ← פתיחה ← מבט מהצד ← פירוק מלא ←
זום על הפרופיל והבידוד. בשביל הסגירה **הסרט מתנגן אחורה** (116 → 0), כך שהסקשן מסתיים
באותה דלת סגורה שבה התחיל. כל הכיתוב שעל המסך נכתב ב־HTML, בעברית תקינה.

**אם הלקוח יפיק את הסרטון מחדש בלי הכתוביות הצרובות** — אפשר להשתמש בכל 221 הפריימים
ולבטל את ההתנגנות לאחור. זה השיפור הבא הכי משתלם.

## חיתוך הפריימים

הסרטון הוא הקלטת מסך ויש בו ממשק נגן. החיתוך `[66, 84, 652, 800]` (x0,y0,x1,y1)
מוריד: תווית „ציר FIRST / 20-40svh" משמאל־למעלה, ה־✕ מימין־למעלה, החצים ‹ › בשני הצדדים
ופס ההתקדמות למטה. לייצור מחדש: `site/scripts/` — הסקריפט לא נשמר; הקוד ב־Python עם
`cv2` + `PIL`, איכות WebP 86.

## מבנה

- הסקשן בגובה `380svh` (‏`340svh` בנייד) → כ־2.8 מסכי גלילה. `.scene-stage` דביק במסך מלא.
- 7 פרקים, כל אחד עם `data-at` = המיקום שלו על ציר הזמן 0..1. הטקסט מתחלף באמצע שבין פרק לפרק.
- `TRACK` ב־`story3d.js` ממפה `t` → מספר פריים. `CALLOUTS` מחזיק את הכיתוב, את צד המסך
  שאליו הוא נצמד ואת נקודת העיגון בתוך התמונה (ב־0..1 של הפריים) בשני פריימי מפתח.
  הקו מצויר ב־SVG מהתווית אל הנקודה, ועוקב אחרי החלק כשהמצלמה מתקרבת.
- טעינה: כל 117 הפריימים נטענים מראש (‏1.75MB) עם פס התקדמות אמיתי. הסקשן עובר לפריסת
  הקולנוע ברגע שהטעינה מתחילה, כדי שלא תהיה קפיצה בפריסה.
- בלי JS / עם „הפחתת תנועה" / אם הטעינה נכשלת: הסקשן נשאר מאמר רגיל עם תמונה ופרקים זה מתחת לזה.

## מה השתנה מסביב

- `site/dist/story.js` — הקוד של לוח הסטוריבורד הישן הוסר. נשאר רק סקשן `#details` ונגן הסרטונים.
- `site/dist/story.css` — כללי `.product-*` שהתייתמו נמחקו.
- `site/dist/lab/door-film/door-model.js` הועבר ל־`site/dist/scene/door-model.js`. נוספו לו:
  אפשרות `quality` להקטנת הטקסטורות, עוצמת normal map יחסית לרזולוציה, וחריצי חיפוי (`joints`)
  בכנף. ה־GLB יוצא מחדש (`site/dist/media/3d/liam-door.glb`, ‏2.75MB).
- `site/scripts/prepare-publish.mjs` — מוסיף גרסה גם ל־`scene/story3d.css` ו־`scene/story3d.js`.

## חוב שנשאר

- **הפריימים של הסרט הישן** (`site/dist/media/door-film/`, כ־17MB) וה־GLB ו־`vendor/three/`
  כבר לא בשימוש באתר עצמו — רק ב־`lab/door-film/`. הם מתפרסמים ל־GitHub Pages ומנפחים את
  הפלט ל־58MB. שווה להחליט אם למחוק את `lab/` ואת המדיה שלו.
- הפריימים שב־`media/door-film/` רונדרו לפני שנוספו חריצי החיפוי למודל, כך שהם לא תואמים
  יותר ל־`door-model.js`. לא משפיע על האתר.

## סקשן `#details` — „פרזול ברמה הגבוהה ביותר"

נבנה מחדש גם הוא (17.09.2026). קודם הייתה שם תמונה דביקה בצד אחד וטור טקסט נגלל בצד השני,
מה שהשאיר חצי מסך ריק בדסקטופ, והכותרת „פרזול ברמה הגבוהה ביותר" הופיעה פעמיים.

עכשיו: כותרת ← **ארבע התמונות** (עלו להתחלה, לבקשת הלקוח) ← רשת של שלושה טורים עם מספור
‏01/02/03, קו דק מעל כל טור והטקסט המקורי. בנייד הטורים נערמים. אין יותר מנגנון דביק.

התוצאה: `story.js` נשאר עם נגן הסרטונים בלבד — **GSAP ו־ScrollTrigger כבר לא נטענים בדף**.
כללי `.detail-visual` / `.detail-step` / `.detail-story` הוסרו מ־`story.css`, ואיתם גם
העקיפה שביטלה את אנימציית ה־`reveal` בסקשנים האלה.
